packages:
  - 'packages/*'
  - 'apps/*'

allowBuilds:
  esbuild: true
  nx: true
  sharp: true

overrides:
  # GHSA-7w5x-hrqm-74c2 (high). nx declares `smol-toml: 1.6.1` exactly at every
  # 23.x release through 23.2.1, so nothing self-heals. Delete when nx unpins.
  smol-toml: ^1.7.1
