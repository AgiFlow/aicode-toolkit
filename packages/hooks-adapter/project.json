{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "@agiflowai/hooks-adapter",
  "projectType": "library",
  "sourceRoot": "packages/hooks-adapter/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "tsdown",
        "cwd": "packages/hooks-adapter"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit",
        "cwd": "packages/hooks-adapter"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "biome check packages/hooks-adapter"
      }
    },
    "lint:fix": {
      "executor": "nx:run-commands",
      "options": {
        "command": "biome check --write packages/hooks-adapter"
      }
    }
  },
  "tags": ["type:library"],
  "sourceTemplate": "typescript-lib"
}
